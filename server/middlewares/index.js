const isAuthenticated = require("./isAuthenticated")
const isAuthorizedQuestion = require("./isAuthorizedQuestion")
const isAuthorizedAnswer = require("./isAuthorizedAnswer")

module.exports = {
  isAuthenticated,
  isAuthorizedQuestion,
  isAuthorizedAnswer
}